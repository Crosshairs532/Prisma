{
  "name": "prisma-1",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/find.ts",
    "create": "ts-node-dev --respawn --transpile-only src/create.ts",
    "aggregate": "ts-node-dev --respawn --transpile-only src/aggregate.ts",
    "groupBy": "ts-node-dev --respawn --transpile-only src/groupBy.ts",
    "relational-query": "ts-node-dev --respawn --transpile-only src/relational-query.ts",
    "rawQuery": "ts-node-dev --respawn --transpile-only src/rawQueries.ts",
    "batch": "ts-node-dev --respawn --transpile-only src/batch.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "nestedWrites": "ts-node-dev --respawn --transpile-only src/nestedWrite.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/node": "^22.8.0",
    "prisma": "^5.21.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "@prisma/client": "^5.21.1",
    "ts-node-dev": "^2.0.0"
  },
  "prisma": {
    "schema": "./prisma/schema.prisma"
  }
}
